---
title: Projects
sidebar_position: 1
---

Project purpose, ownership, and related work in one place. One page per team repository.

Each page has two parts. A facts block (stack, repository, recent commits) is kept current by `npm run sync` on a machine with the repositories checked out. The narrative sections are written by the `ds-wiki-project` skill from the repository itself. Edit prose freely; the sync only rewrites what sits between its markers.

<!-- sync:projects:start -->
No projects are registered in `sync.config.local.json` on this machine.
<!-- sync:projects:end -->

:::note[Awaiting original content]
The original project pages have not been imported from ds-app. Register repositories in `sync.config.local.json` and run `ds-wiki-project` to build this section out.
:::
